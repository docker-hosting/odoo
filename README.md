# Odoo template for docker-hosting

Just clone this repo and create a `.env` file with the following environment variable (these will be filled in the docker compose file):

```bash
URL=www.my-website.com
```
